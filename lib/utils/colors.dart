import 'package:flutter/material.dart';

// Gradiente para o aluno
const LinearGradient alunoGradient = LinearGradient(
  colors: [
    Color(0xFFFF4F00), // Cor inicial
    Color(0xFFFF7B5C), // Cor final
  ],
  begin: Alignment.bottomRight, // Gradiente começando da esquerda
  end: Alignment.topLeft,  // Terminando à direita
);

const alunoCor = Color(0xFFFF4F00);
const alunoCor2 = Color(0xFFFF7B5C);
const secondaryBlack = Color(0xFF797979);
const lightBlue = Color(0xFFF4F6F9);

const personalColor = Color(0xFF134C74);